---
title: 문서 제목
project: 프로젝트명
version: v0.1
status: draft
updated: 2026-01-01
keywords: 키워드1, 키워드2, 키워드3
summary: 카드에 보일 한 줄 요약
---

<!--
  ┌──────────────────────────────────────────────────────────────────┐
  │ 기획서 작성 규격 (SPEC FORMAT) — 모든 기획서는 이 형식을 따른다.    │
  └──────────────────────────────────────────────────────────────────┘

  새 기획서를 만들 때 이 파일을 복사해서 content/specs/<slug>.md 로 저장한다.
  - <slug> 은 영문 소문자/숫자/하이픈만 사용 (URL 이 됨). 예: deokji-rebranding
  - `_` 나 `.` 로 시작하는 파일은 목록에 노출되지 않는다 (이 템플릿처럼).

  ── frontmatter (위쪽 --- 블록) ──────────────────────────────────────
  필드는 평평한 `key: value` 만 쓴다 (중첩 X). 모두 채울 것.
    title    문서 제목 (카드/표지에 표시)
    project  소속 프로젝트. 같은 project 끼리 묶여서 그룹으로 보인다.
    version  현재 버전. "v0.1" 형식. changelog 의 최신 version 과 맞춘다.
    status   draft | review | final 중 하나
    updated  마지막 수정일 ISO (YYYY-MM-DD). 목록은 이 날짜로 최신순 정렬.
    keywords 쉼표로 구분한 키워드들
    summary  카드에 보일 한 줄 요약

  ── 본문: 페이지(deck) 구조 ──────────────────────────────────────────
  본문은 `---` 로 구분되는 페이지들의 연속이다.
  각 페이지는 맨 위에 타입 디렉티브로 종류를 선언한다: <!-- type: 종류 -->
    cover    표지     맨 첫 페이지. # 제목 + 부제(자유 마크다운)
    divider  간지     챕터 구분. # N. 챕터제목 만.
    policy   정책     상위 정책/배경/시퀀스 등 자유 마크다운.
    detail   상세기획 프로토타입 이미지(좌) + 번호별 요소 정의(우).

  ── 블록 안정 키 (stable key) ────────────────────────────────────────
  모든 페이지는 안정 키를 가진다 — 페이지 순서를 바꿔도 유지되므로 changelog·
  diff·코멘트가 특정 블록을 가리킬 때 쓴다.
    · 기본값: 타입 + 제목에서 자동 생성 (예: "detail-로그인-화면").
    · 명시 지정: <!-- id: login-screen --> 를 페이지 맨 위에 두면 제목을 바꿔도
      키가 유지된다. changelog 에서 자주 참조할 블록은 명시해두는 게 안전하다.
  changelog(<slug>.changelog.json)의 entries[].block 에 이 키를 넣으면 된다.

  ── detail 페이지 작성 규칙 ──────────────────────────────────────────
    <!-- type: detail -->
    <!-- medium: app -->        ← app(세로) | web(가로). 프로토타입 비율/뱃지 결정. 생략시 web.
    # 화면 이름
    ![](/wireframe.png)         ← 프로토타입 이미지(선택). 없으면 placeholder 표시.
    1. **요소 이름** — 이 요소의 정책/동작 설명
    2. **다음 요소** — 설명
    번호는 프로토타입 위 핀과 우측 정의 테이블에 함께 쓰인다.

  아래는 각 타입을 하나씩 보여주는 스켈레톤이다. 필요한 만큼 복제/삭제해서 쓴다.
  (이 주석 블록과 안내 문구는 실제 작성 시 지운다.)
-->

<!-- type: cover -->

# 문서 제목

서비스/기능 한 줄 설명 (부제)

---

<!-- type: divider -->

# 1. 기획 배경

---

<!-- type: policy -->

# 기획 배경

상위 정책 · 배경 · 목표 · 시퀀스 다이어그램 등을 자유 마크다운으로 정리한다.

- 핵심 목표 1
- 핵심 목표 2

---

<!-- type: divider -->

# 2. 상세 기획

---

<!-- type: detail -->
<!-- medium: app -->
<!-- id: screen-name -->

# 화면 이름

![](/wireframe.png)

1. **첫 번째 요소** — 이 요소가 무엇이고 어떻게 동작하는지 정책 설명
2. **두 번째 요소** — 설명
3. **세 번째 요소** — 설명
